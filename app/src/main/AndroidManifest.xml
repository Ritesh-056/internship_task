<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.internshipproject">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.orm.SugarApp"
        tools:replace="android:icon"
        android:theme="@style/Theme.InternshipProject">


        <meta-data android:name="DATABASE" android:value="interns.db" />
        <meta-data android:name="VERSION" android:value="7" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.example.internshipproject" />

        <activity
            android:name=".AddCommentToApiActivity"
            android:exported="true" />
        <activity
            android:name=".Screens.PostDetailScreenActivity"
            android:exported="true" />
        <activity
            android:name=".Screens.PostScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>